diff --git a/autolab/research.yaml b/autolab/research.yaml
index 7641b6d..1c3a25a 100644
--- a/autolab/research.yaml
+++ b/autolab/research.yaml
@@ -1,5 +1,5 @@
-name: nanochat-speedrun
-eval_version: "nanochat-eval@0.1.0"
+name: nanochat-4070-pilot
+eval_version: "nanochat-4070-pilot-eval@0.1.0"
 
 # The single mutable file, per the upstream design: everything in train.py is
 # fair game (architecture, optimizer, hyperparameters); prepare.py and the
@@ -43,4 +43,3 @@ shares:
 
 # On gmacko-mini the main container runs near-full; keep worktrees on the
 # roomier SSD. Linux GPU workers can override or drop this.
-worktree_base: /Volumes/dev-ssd/autolab-worktrees
diff --git a/train.py b/train.py
index 2e74397..b6a2d08 100644
--- a/train.py
+++ b/train.py
@@ -435,7 +435,7 @@ HEAD_DIM = 128          # target head dimension for attention
 WINDOW_PATTERN = "SSSL" # sliding window pattern: L=full, S=half context
 
 # Optimization
-TOTAL_BATCH_SIZE = 2**19 # ~524K tokens per optimizer step
+TOTAL_BATCH_SIZE = 2**16 # ~524K tokens per optimizer step
 EMBEDDING_LR = 0.6      # learning rate for token embeddings (Adam)
 UNEMBEDDING_LR = 0.004  # learning rate for lm_head (Adam)
 MATRIX_LR = 0.04        # learning rate for matrix parameters (Muon)
@@ -447,16 +447,17 @@ WARMDOWN_RATIO = 0.5    # fraction of time budget for LR warmdown
 FINAL_LR_FRAC = 0.0     # final LR as fraction of initial
 
 # Model size
-DEPTH = 8               # number of transformer layers
-DEVICE_BATCH_SIZE = 128  # per-device batch size (reduce if OOM)
+DEPTH = 4               # number of transformer layers
+DEVICE_BATCH_SIZE = 8  # per-device batch size (reduce if OOM)
 
 # ---------------------------------------------------------------------------
 # Setup: tokenizer, model, optimizer, dataloader
 # ---------------------------------------------------------------------------
 
 t_start = time.time()
-torch.manual_seed(42)
-torch.cuda.manual_seed(42)
+research_seed = int(os.environ.get("AUTOLAB_SEED", "42"))
+torch.manual_seed(research_seed)
+torch.cuda.manual_seed(research_seed)
 torch.set_float32_matmul_precision("high")
 device = torch.device("cuda")
 autocast_ctx = torch.amp.autocast(device_type="cuda", dtype=torch.bfloat16)
